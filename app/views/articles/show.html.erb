<div class="container mx-auto my-8">
  <h1 class="text-3xl font-bold mb-4"><%= @article.title %></h1>
  <p class="mb-2">Status: <%= @article.status.capitalize %></p>
  <div class="prose mb-4"><%= @article.content %></div>

  <%= link_to 'Edit', edit_article_path(@article), class: 'bg-yellow-500 text-white py-2 px-4 rounded-lg hover:bg-yellow-700 mr-2' %>
  <%= link_to 'Back to Articles', articles_path, class: 'bg-gray-500 text-white py-2 px-4 rounded-lg hover:bg-gray-700' %>
</div>
